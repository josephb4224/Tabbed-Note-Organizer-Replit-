## Packages
(none needed)

## Notes
- Using framer-motion for smooth drawer/tab transitions (already installed)
- Using date-fns for timestamp formatting (already installed)
- Using react-hook-form + zod for validation (already installed)
- Categories have a hex color field which is used for the UI accents
